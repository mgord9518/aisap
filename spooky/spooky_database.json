{
    "TODO": "Read this file in isspooky.go",
    "paths": [
        "/",
        "/etc",
        "/home",
        "${HOME}",
        "${HOME}/.profile",
        "${HOME}/.bashrc",
        "${HOME}/.zshrc"
    ],
    "trees": [
        "${HOME}/apps",
        "${HOME}/Apps",
        "${HOME}/AppImages",
        "${HOME}/appimages",
        "${HOME}/Applications",
        "${HOME}/applications",
        "${HOME}/go",
        "${HOME}/.ssh",
        "${HOME}/.vim",
        "${HOME}/.gnupg",
        "${HOME}/.firefox",
        "${HOME}/.mozilla",
        "$HOME/.cache",
        "$HOME/.local",
        "$HOME/.config",
        "/run",
        "/tmp",
        "${XDG_CACHE_DIR}",
        "${XDG_DATA_DIR}",
        "${XDG_CONFIG_DIR}",
        "${XDG_RUNTIME_DIR}",
        "${TMPDIR}"
    ]
}
